import 'package:flutter/material.dart';

class BlogCategoryRow extends StatelessWidget {
  const BlogCategoryRow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
